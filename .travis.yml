language : node_js
node_js :
 - stable
install:
 - npm install
script:
  - if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then npm test:osx          ; fi
  - if [[ "$TRAVIS_OS_NAME" == "linux" ]]; then npm test:lin        ; fi
  - if [[ "$TRAVIS_OS_NAME" == "win" ]]; then npm test:win          ; fi
